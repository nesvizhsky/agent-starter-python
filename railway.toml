# Deploy config for Eat the Elephant.
# Both services (eat-the-elephant and elephant-cron-tick) build from the same
# Dockerfile. The startup script dispatches to the right command per service.
[deploy]
startCommand = "bash scripts/railway_start.sh"
